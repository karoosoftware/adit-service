include:
  - local: .ci/terraform.yml
    rules:
      - changes:
          - infra/**/*
          - .ci/terraform.yml
  - local: .ci/app.yml
    rules:
      - changes:
          - src/**/*
          - docker/**/*
          - .ci/app.yml
